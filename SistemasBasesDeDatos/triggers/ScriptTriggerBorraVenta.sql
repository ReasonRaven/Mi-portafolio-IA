DELIMITER //

CREATE TRIGGER borra_venta
BEFORE DELETE ON VENTA
FOR EACH ROW
BEGIN

    DELETE FROM DET_VENTA
    WHERE NUM_VENTA = OLD.NUM_VENTA;

    DELETE FROM BACKORDER
    WHERE NUM_VENTA = OLD.NUM_VENTA;

END//

DELIMITER ;

